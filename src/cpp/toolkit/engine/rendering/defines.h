#pragma once

namespace tk {
namespace engine {
    enum class RenderMode
    {
        Triangles,
        Lines,
    };

    enum class FillMode
    {
        Fill,
        Line,
    };

    enum class DepthFunc
    {
        Always,
        Less,
    };

}
}
